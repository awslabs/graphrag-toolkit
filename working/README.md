# working/

Local scratch area. This entire folder is git-ignored (see `/working/` in `.gitignore`)
and is never committed or pushed. Use it for experiments, converted examples, notes,
throwaway data, and anything you don't want tracked.
